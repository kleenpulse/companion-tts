# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local agent state
.claude/settings.local.json

# Local launch playbook — never ships
docs/LAUNCH.md

# Machine-specific build env (copy from config.toml.example)
src-tauri/.cargo/config.toml

# Env files — keys belong in the settings UI or your shell, never in git
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
